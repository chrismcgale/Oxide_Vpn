---
name: oxide-vpn
description: Operational runbook for the Oxide VPN — a WireGuard-based (boringtun) VPN platform in Rust. Run, debug, architecture pointer, gotchas, changelog.
---

# Oxide VPN runbook

A WireGuard data plane (via **boringtun**) with an Oxide-built control/privacy/ops
layer on top. Modeled on Mullvad / NordLynx. Linux-first.

**Forward plan:** the M1–M4 rearchitecture is done. The active execution plan — current
state, working conventions, and ~6 months of ordered work (Wave 1 moonshots: DAITA →
undetectable server → onion multihop → verifiable no-logs; then scale, clients, research)
— lives in the repo at **`PLAN.md`**. Read that to know what to build next; read this file
to build/run/debug. (Historical M1–M6 plan: `~/.claude/plans/wobbly-splashing-puddle.md`.)

Toolchain note: Rust is installed via **rustup** in `~/.cargo` (not a system package).
Run `. "$HOME/.cargo/env"` first in a fresh shell, or use `~/.cargo/bin/cargo`.

## Run

```bash
cargo build                       # whole workspace
cargo test --workspace            # unit tests (keys, config, router)
cargo clippy --workspace

# Key management (interoperable with `wg genkey`/`wg pubkey`):
cargo run -p oxide-serverd -- genkey                 # print a private key
echo "<privkey>" | cargo run -p oxide-serverd -- pubkey

# Data plane (needs root / CAP_NET_ADMIN):
sudo target/debug/oxide-serverd up --config server.toml
sudo target/debug/oxide-client  up --config client.toml

# Control plane (M2/M3 — no root; plain web service + SQLite):
target/debug/oxide-control-plane --db oxide.db add-server \
    --id us-1 --public-key <server-pubkey> --endpoint 1.2.3.4:51820 --cidr 10.8.0.0/24 \
    --country US --city "New York" --capacity 500      # M3: location + capacity
target/debug/oxide-control-plane --db oxide.db serve --listen 127.0.0.1:8080
# Client via control plane (register device, get assigned IP, connect):
target/debug/oxide-client account --control-plane http://cp:8080          # new account number
# Explicit server, or auto-select the least-loaded (optionally by location):
sudo target/debug/oxide-client connect --control-plane http://cp:8080 --account <n> --server us-1
sudo target/debug/oxide-client connect --control-plane http://cp:8080 --account <n> --country US
# Multihop: tunnel to the exit through an entry relay (entry auto-picked if omitted):
sudo target/debug/oxide-client connect --control-plane http://cp:8080 --account <n> --exit se-1 --entry de-1

# TUI client (agent runs as root; TUI runs as your user):
sudo target/debug/oxide-agentd &                  # privileged agent on /run/oxide/agent.sock
target/debug/oxide-tui --control-plane http://cp:8080 --account <n>   # ↑/↓ select · Enter connect · d disconnect · q quit
```

Config templates: `configs/server.toml.example`, `configs/client.toml.example`.
Put real configs somewhere gitignored (`*.local.toml` or `run/`).

## Verify (end-to-end)

No-root tests — the whole stack is exercised in userspace (mock TUN + loopback UDP +
in-process control plane):

```bash
cargo test --workspace                                  # everything (19 tests)
cargo test -p oxide-wg-core --test tunnel               # real handshake over loopback
cargo test -p oxide-control-plane --test api            # account/device/IP/peer API + server selection
cargo test -p oxide-serverd --test control_plane_flow   # CP -> reconcile -> tunnel (capstone)
cargo test -p oxide-serverd --test multihop_flow        # client -> entry relay -> exit (multihop capstone)
cargo test -p oxide-relay                               # UDP relay forwarding + flow isolation
cargo test -p oxide-obfs                                # obfuscation codec
cargo test -p oxide-wg-core --test tunnel tunnel_works_over_obfuscated_transport  # stealth capstone
```

Fastest full (privileged) test — two network namespaces on one host, real handshake +
ping across the tunnel:

```bash
sudo bash scripts/netns-test.sh      # prints PASS/PASS ✗ and both daemon logs
```

Manual two-host verification (Milestone 1 definition of done):
- **A handshake:** `ip addr show oxide0`; `tcpdump -ni <wan> udp port 51820` shows WG init/response.
- **B tunnel ping:** ping the peer's tunnel IP; `tcpdump -i oxide0 icmp` shows plaintext ICMP while `<wan>` shows only encrypted UDP.
- **C full tunnel:** from the client, `curl ifconfig.co` returns the **server's** IP; `nft list ruleset` shows the `oxide` masquerade table; `ip route get 1.1.1.1` resolves via `oxide0`.
- **D interop:** our key derivation matches stock `wg pubkey` (verified in CI-style check). A stock `wg` peer should handshake with our daemon.

## Debug / common errors

- **`Operation not permitted` bringing up tun** — not root / missing `CAP_NET_ADMIN`. Run under `sudo`.
- **Handshake never completes** — check the timer task is ticking (boringtun needs `update_timers` on a cadence — see `wg-core/src/engine.rs::timer_loop`). Confirm UDP reaches `:51820` (firewall). Confirm keys: client's `[[peer]].public_key` must be the server's public key and vice-versa.
- **Ping works but curl/large transfers hang** — MTU / PMTU black hole. The NAT ruleset now MSS-clamps forwarded TCP to the route MTU (`nat.rs`), which fixes the common case. If it persists, lower the tunnel MTU (and lower it further under stealth).
- **Full tunnel connects then the connection dies** — the encrypted UDP is routing into the tunnel. The client pins a `/32` host route to the server endpoint via the original gateway *before* swinging the default (`netlink::add_host_route_via`). Verify with `ip route get <server-ip>`.
- **NAT egress silently drops replies** — strict `rp_filter`. We set it to loose (2); confirm `sysctl net.ipv4.conf.all.rp_filter`.
- **`nft` table left behind after a crash** — `sudo nft delete table inet oxide` (NAT) or `sudo nft delete table inet oxide-ks` (kill switch).
- **Kill switch locked me out / SSH froze** — the kill switch drops all non-tunnel output. On a remote box this can cut your session. Remove it with `sudo nft delete table inet oxide-ks`. It only permits loopback, the tunnel, and UDP to the server endpoint.
- **DNS didn't change / reverted** — we now detect `systemd-resolved` (stub at `/run/systemd/resolve/stub-resolv.conf`) and use `resolvectl` per-link DNS + `~.` routing domain; check `resolvectl status oxide0`. If a bare NetworkManager (no resolved) manages DNS it may still reassert on a network change — NM-D-Bus backend is future work. The resolv.conf fallback applies only when resolved isn't running.

## Architecture (brief)

Cargo workspace, `crates/`:
- **`common`** — key newtypes (zeroizing, base64, `wg`-compatible), TOML config, error, the `TunQueue` trait, anonymous account numbers, the control-plane API DTOs, and the TUI **profile loader** (`profile`: `~/.config/oxide/*.toml`). No tokio/OS deps.
- **`wg-core`** — the engine: wraps boringtun `Tunn`; three tokio tasks (outbound TUN→UDP, inbound UDP→TUN, 250ms timers) sharing `Arc<Shared>`; allowed-IPs router; **runtime-mutable peer table** (`EngineHandle::{add_peer,remove_peer,reconcile}`). OS-agnostic (talks to TUN via `TunQueue`). `test-util` feature exposes a mock TUN.
- **`net`** (`oxide-net`) — **platform facade**: re-exports the privileged network backend by build target (`#[cfg(target_os=…)]` + target-gated deps) so the client crates depend only on this, not a specific OS crate. The re-exported surface is the **client contract** every backend provides (`TunDevice`, `Netlink`, `bring_up_interface`, `bind_dual_stack`, `shutdown_signal`, `dns`, `killswitch`).
- **`net-linux`** — privileged Linux bits: TUN `ioctl` + `AsyncFd`; **link/addr/route via real netlink** (`netlink.rs`, rtnetlink — `Netlink` handle, async, family-agnostic so IPv6 works); kill switch via **`rustables`** (netlink); NAT via `nft` + `sysctl` shell out; **DNS leak protection** (`dns.rs`). `bring_up_interface` is async and returns the link index. Also holds the **server-only** bits (`nat`, `sysctl`) that `oxide-serverd` uses directly.
- **`net-macos`** (`oxide-net-macos`) — macOS sibling backend, `#![cfg(target_os="macos")]` (empty on Linux). **Scaffold, cross-compile-checked only** (no Mac here): utun `TunDevice` (PF_SYSTEM/SYSPROTO_CONTROL + 4-byte AF header), `bind_dual_stack`/`shutdown_signal`/`dns`(resolv.conf) done; `Netlink` routing (BSD PF_ROUTE) + pf kill switch stubbed `Unsupported`+`TODO(macos)`.
- **`net-windows`** (`oxide-net-windows`) — Windows sibling backend, `#![cfg(target_os="windows")]` (empty on Linux). **Scaffold, cross-compile-checked only** (no Windows here): `bind_dual_stack`/`shutdown_signal` done; `TunDevice` (Wintun DLL), `Netlink` (IP Helper API/netsh), `dns` (SetInterfaceDnsSettings), WFP kill switch all stubbed `Unsupported`+`TODO(windows)`.
- **`control-plane`** — axum + sqlx service on **SQLite or Postgres** (chosen by the connection string): accounts (anonymous numbers), devices (pubkey + assigned tunnel IP), servers (with location + capacity), IP allocation, **load-based server selection** (`GET /v1/servers/best`) fed by **server heartbeats**. Never depends on `wg-core`. `add_server` CLI + `serve`. Backend abstraction lives in `db.rs` (`Db`/`DbRow`/`Val` dialect layer — SQL written once, `?`→`$n` for PG); switching is a connection-string change.
- **`control-client`** — thin reqwest client for the control-plane API, shared by both daemons.
- **`relay`** — a pure-tokio UDP relay used by **multihop** entry servers: forwards a client's WireGuard ciphertext to the exit server (per-client upstream flows), so no single server sees both the client's IP and its destination. No root; unit-tested over loopback.
- **`pq`** — **post-quantum** key agreement (ML-KEM-768 via `ml-kem`). `generate`/`encapsulate`/`decapsulate` produce a 32-byte shared secret used as the WireGuard **PSK**, layered on top of x25519 — strictly additive/hybrid (can only strengthen; a flaw in the unaudited ML-KEM can't weaken the classical security). Pure; unit-tested. Capstone: a real tunnel secured by the PQ-derived PSK.
- **`mimicry`** — **stealth tier 2 (protocol mimicry)**. Two modes, both making the flow look like normal web traffic (blocking it = blocking HTTPS/HTTP3):
  - **TLS-over-TCP** (`mimicry` root + `wg-core::MimicTransport`, `Transport::Mimic`): a real-looking TLS 1.3 handshake (ClientHello w/ SNI, ServerHello) then datagrams as TLS `application_data` records over TCP (server accept + demux by TCP source addr).
  - **QUIC-over-UDP** (`mimicry::quic` + `Transport::QuicMimic`): UDP-native (no TCP-over-TCP), looks like HTTP/3 — first packet a QUIC Initial (v1, connection IDs, embedded ClientHello, padded to 1200), rest short-header. Reuses the datagram model.
  Payloads are obfuscated first, so record/packet contents are high-entropy like real TLS/QUIC. It's mimicry, not a full protocol emulator (fools passive fingerprinting, which is what blocks WireGuard); security is the WG AEAD inside.
  - **Decoy-forwarding** (`wg-core::decoy`, Sprint 1B): the QUIC transport's `quic_mimic_with_decoy` proxies an unauthenticated first-contact Initial to a real backend (`DecoyForwarder`, per-source UDP upstream + reply pump through the listen socket) so the port answers like an ordinary QUIC server under active probing, instead of the silent drop. Config: `interface.decoy_backend`.
  - **Transport selection** (Sprint 2A): `common::TransportKind` (`plain|obfs|quic|mimic`) + `interface.transport`/`interface.daita`; daemons build the selected transport (`oxide-serverd::build_server_transport`, `client-core::build_client_transport`). Back-compat: `obfuscation_key` alone = `obfs`. Control-plane distribution of the choice is a follow-up (2A-2).
- **`obfs`** — **stealth mode** codec: wraps each datagram as `[nonce][ChaCha20(key,nonce) XOR ([len][payload][pad])]` so DPI can't fingerprint WireGuard (no fixed header). Padding rounds the datagram up to size **buckets** (256/512/…/1472) so handshakes/keepalives/small data collapse into indistinguishable sizes — traffic-analysis resistance (size dimension). Pure; unit-tested. Used by `wg-core`'s `Transport::Obfuscated`. (Timing-based cover traffic and protocol *mimicry* — WG-in-TLS/QUIC — are the next tiers.)
- **`daita`** — **traffic-analysis defense (DAITA v1)**: the pure cell codec + shaping decision. A **cell** is `[type:1][len:2][payload][random pad]` padded to a fixed `cell_size`, `type` = `REAL` (a WG datagram) or `COVER` (droppable filler). The `Shaper` is a bounded queue drained **one cell per slot** — a real datagram if queued, else a cover cell. Cells are the *plaintext* the obfs frame wraps, so a fixed cell size ⇒ one obfs bucket ⇒ one wire size, and recognizing/dropping cover needs the shared obfs key (so DAITA implies stealth). Wired into `wg-core` via `Engine::with_daita(Daita::{shaping,framing})` — the client shapes egress to a constant rate + cover; the server frames replies and drops inbound cover before boringtun. Honest cost: constant `cell_size*8/slot` bandwidth each way. Pure/unit-tested; capstone `tunnel_works_with_daita_shaping`.
- **`seccomp`** (`oxide-seccomp`) — **enforced no-logs (1D-1)**: builds + applies a seccomp-BPF filter that makes the process unable to create/write files (gates `openat`/`open` *flags* + FS-mutating syscalls; default-allow otherwise, so sockets/read-only opens/stdout work). `apply_no_disk_writes()`; serverd runs it under `[hardening] no_disk_writes`. Unprivileged (NO_NEW_PRIVS), fork-tested.
- **`attest`** (`oxide-attest`) — **verifiable build identity (1D-2/1D-3)**: Ed25519 signed `BuildManifest` (binary SHA-256 + git commit + version) verified against a **pinned** release key (`sign_manifest`/`verify_manifest`, `self_binary_sha256`), plus a hash-chained append-only `TransparencyLog` of deployed builds with a signed head (`verify_log`). Pure crypto; serverd exposes `attest-genkey`/`manifest`.
- **`oxide-serverd`** — server daemon: config → engine → net-linux; polls the control plane and reconciles peers live. Also: `genkey`/`pubkey`/`pq-genkey`/`attest-genkey`/`manifest` subcommands; `build.rs` bakes in git commit + build time.
- **`client-core`** — shared client tunnel logic: `resolve_connection` (control-plane server selection + PQ + registration → local config, unprivileged), `resolve_mesh` (join the account's private mesh → local config with one peer per device, unprivileged), `run_tunnel` (bring up TUN + routing + kill switch + DNS + engine until a caller-supplied `stop`, with an `on_ready` callback exposing the `EngineHandle` for live stats + an optional liveness watchdog), and `run_supervised` (**always-on**: reconnect + fail over to another server on link death, backoff, until stopped). Reliability logic (liveness/backoff/failover selection) lives in the pure, unit-tested `client-core::reconnect`; server **latency** estimation (no-root "TCP ping") in `client-core::latency`.
- **`oxide-client`** — thin CLI over `client-core` (`up`/`connect`/`mesh`/`account`/`genkey`).
- **`oxide-agentd`** — privileged agent: runs as root, owns the tunnel (embedded via `client-core`), serves a Unix-socket control API (newline-JSON: Status/Connect/Disconnect/NewIdentity) so the UI never needs root. Tracks a **lifecycle phase** cell + per-connection meta from the `ConnEvent` stream, surfaced in `TunnelStatus` (phase, handshake age, transport/DAITA/kill-switch flags).
- **`oxide-tui`** — unprivileged terminal UI (ratatui): browse servers, connect/disconnect/new-identity via the agent socket, and a **phase-aware live status header** — braille spinner while working, link-health dot (from handshake age), capability badge row (transport/DAITA/PQ/STEALTH/KILL), throughput sparklines. Connect UX: **kill-switch toggle** (K), **filter/search** (`/` over id/country/city), **quick-connect-best** (b), and **latency ranking** (p = concurrent `client-core::latency` "TCP ping" sweep; t sorts by it). Selection runs over the pure `App::visible()` (filtered/sorted) list. Loads `~/.config/oxide/client.toml` (via `oxide_common::profile`). Pure render/logic helpers + `TestBackend` render tests.
- **`oxide-admin-tui`** — unprivileged **operator console** (ratatui) over the control plane's admin API (`GET /v1/admin/overview` + `/v1/admin/servers`, admin-token-gated): tabbed Overview (fleet stat cards + estimated cost), Servers (load bars + feature badges + **detail pane** + per-server cost), Features (adoption gauges). **Alerts** (`server_alert`: STALE / HIGH LOAD) → row markers + header chip; **rotate-token** action (R, confirmed) via `admin_rotate_token`. Loads `~/.config/oxide/admin.toml`. Fleet-level aggregates only — never per-account data. Pure cost/adoption/alert helpers + render tests.

Client UX architecture: **unprivileged TUI ⇄ Unix socket ⇄ privileged agent**. The agent does all root-requiring work; the TUI is a pure client of the control plane + agent socket.

Control-plane <-> server sync: the server **polls** `GET /v1/internal/servers/{id}/peers` (token-authed) and reconciles into the live engine. The data plane keeps peer state in RAM only; the DB lives solely in the control plane.

Data flow and boringtun contracts are documented at the top of `crates/wg-core/src/engine.rs`.

## Gotchas

- **boringtun contract 1:** `update_timers` MUST be called on a ticker or handshakes never retry.
- **boringtun contract 2:** after `decapsulate` returns `WriteToNetwork`, keep calling `decapsulate(None, &[], buf)` until `Done`, sending each datagram.
- **Never hold the `Tunn` mutex across `.await`** — it's a blocking `std::sync::Mutex`; lock, run one boringtun call into a stack buffer, copy out, unlock, then await I/O.
- **`Engine::run`'s spawned tasks must die with it** — they hold `Arc<Shared>` (and the TUN). An `AbortGuard` aborts them when `run` returns/cancels; otherwise a supervisor that drops the run future leaks the tasks and the interface (EBUSY on the next reconnect). Any new spawned engine task must be registered with that guard.
- **The data plane must ride through a single send failure** — `outbound_loop` drops-and-continues on a send error (a firewall/kill-switch drop makes `sendto` return EPERM); it must never propagate and tear down the engine.
- **Peer demux is by source-address cache with an all-peers fallback** (M1). A proper receiver-index table is an M2 refinement; matters once one server has many clients.
- **No-logs posture:** the data plane keeps nothing on disk; never `tracing`-log secret key material (the `SecretKey` type refuses to print its bytes). Any persistence belongs in the control plane (M2+), never here.

## Privacy posture (no-logs / leak prevention)

- **Kill switch** (`--kill-switch`): an nft `output` chain (policy drop) permitting only loopback, the tunnel interface, and the encrypted UDP to the server. Nothing leaks in the clear if the tunnel drops. Removed on disconnect.
- **DNS leak protection**: while connected, `/etc/resolv.conf` is repointed at the tunnel DNS (handed out by the control plane per-server, or set in client config) and restored on disconnect. Caveat: `systemd-resolved`/NetworkManager may reclaim the file — a resolved-aware backend is future work.
- **No-logs / RAM-only (audited 2026-07-21):** the data plane (`wg-core`, `oxide-serverd`, `net-linux`) performs **no disk writes** — peers live in RAM only. The engine logs no client PII at the default level (pubkeys/endpoints are `debug`-only). The control-plane DB stores only routing essentials (account numbers, device pubkeys, IP assignments) — no traffic/activity logs. Client-side disk writes are limited to the device key (0600) and the resolv.conf swap, both intentional and local.

## Changelog / Decisions (newest first)

- **2026-07-29 — Wave 4 opened: 4A adaptive DAITA pacing (maybenot-style, first step).** DAITA v1 emits one cell per **fixed** slot (a flat constant rate — zero timing leak but full `cell_size/slot` bandwidth always). 4A adds a pure **state machine** (`daita::pacer::Pacer`, states Active/Idle) that instead decides the **delay before each cell**: (1) **jittered timing** — each delay is jittered ±25%, so there's no fixed inter-cell interval for an ML classifier to lock onto, at the *same average* rate while active; (2) **idle taper** — after `idle_after` cover-only cells the cadence grows toward `slot*max_mult`, cutting an idle tunnel's constant-bandwidth cost. **Honest trade:** the taper leaks the coarse active/idle *envelope* (an observer sees the rate change) — the classic maybenot cost/leak knob; constant-rate stays the choice for zero envelope leak. The state logic is pure + unit-tested (active→~slot / threshold / cap / real-resets); only `jitter()` uses the OS RNG. **Wiring:** `Daita` gains an optional `pacer: Option<Mutex<Pacer>>` + `client_adaptive()`; the engine `shaper_loop` sleeps the pacer's `next_delay(last_was_real)` when present, else the fixed `interval(slot)` (constant-rate). Opt-in via `interface.daita_adaptive` (client-only, requires `daita`, validated) → `client-core` selects `Daita::client_adaptive()`. Loopback test: an adaptively-paced DAITA tunnel still carries a packet + still drops cover. **Deferred (the rest of 4A):** the full maybenot machine framework (multiple states + sampled distributions per state), **bidirectional** shaping (server generates cover, not just framing), per-hop/multihop cover, and CP distribution of the adaptive choice. 180 tests.
- **2026-07-29 — 2H follow-on: obfs-decode + decoy-forward counters (the last deferred 2H item).** The stealth/probe-resistance defenses now have runtime counters, closing the gap the lean 2H pass left. These live in the **Transport** layer (not the engine's `Shared`), so `Transport` became a struct `{ wire, counters: Arc<TransportCounters> }` (the variant enum is now the private `Wire`; nothing external matched on it) with two `Relaxed` atomics incremented on the recv path: **`obfs_decode_failures`** (undecodable inbound datagrams the obfs layer drops — scans/probes/junk on the port) and **`decoy_forwards`** (unauthenticated QUIC Initials spliced to the decoy backend — active-probe deflections, server-side). `Transport::counters()` snapshots them; they flow `EngineStats` → `HeartbeatRequest` → serverd → 2 raw-latest `servers` columns → `/metrics` (`oxide_server_obfs_decode_failures_total`, `oxide_server_decoy_forwards_total`, counter) + `AdminServerInfo` → admin-TUI detail (`stealth: obfs-drop N · decoy N`, shown for a stealth server). Tests: the existing decoy test now asserts `decoy_forwards`; a new `obfs_decode_failures_are_counted` feeds a junk datagram then a valid one; CP metrics/admin tests + admin-TUI render test extended (trimmed a blank in the detail pane to keep its ~17-row height). Live PG green. 175 tests. **2H's deferred counters are now all done** (only external Grafana dashboards remain out — user prefers TUI).
- **2026-07-29 — 3B: `net-windows` scaffold (cross-compile-checked).** The Windows sibling to `net-linux`/`net-macos`, wired into the `oxide-net` facade's Windows arm. Same pattern as `net-macos`: `#![cfg(target_os="windows")]` (empty on Linux), mirrors the client contract, verified via `cargo check --target x86_64-pc-windows-gnu` (compile-checked, not runtime — no Windows here). Windows diverges more than macOS — there's no TUN fd and no `resolv.conf` — so more is stubbed: **done** = `bind_dual_stack` (socket2) + `shutdown_signal` (`tokio::signal::ctrl_c`); **stubbed `Unsupported`+`TODO(windows)`** = `TunDevice` (Wintun DLL + ring buffers via the `wintun` crate — reads block on an event, so the async `TunQueue` needs a blocking thread bridged to tokio), `Netlink` routing/addressing (IP Helper API `CreateUnicastIpAddressEntry`/`CreateIpForwardEntry2`/`GetBestRoute2` or netsh), `dns` (`SetInterfaceDnsSettings`/netsh), and the kill switch (Windows Filtering Platform / `netsh advfirewall`). Compiles + links so the client cross-compiles; can't run until implemented + live-verified on Windows. Linux workspace unchanged (174 tests, clippy `-D` clean on host + windows-gnu target).
- **2026-07-29 — 4B continuous rekey: runtime PQ-PSK rotation (mechanism DONE; client driver deferred).** Rotate the PQ-derived WireGuard PSK on a live tunnel for forward secrecy beyond WG's ephemeral rekey. **Constraint that shaped the design:** boringtun fixes the PSK at `Tunn::new` (no runtime setter) and `reconcile`/`add` no-op on an existing peer — so a rotation must *recreate* the session. **wg-core:** new `EngineHandle::replace_peer(params)` removes a peer's `Tunn`+routes and adds a fresh one (new PSK, same endpoint/allowed-ips/keepalive) under one write lock, pruning the source-addr/receiver-index demux caches (refactored out of `remove_peer` into `prune_caches`); `PeerParams` derives `Clone`. Loopback test proves both ends rotating to a new PSK → a fresh handshake resumes traffic, and a one-sided rotation breaks delivery (PSK enforced end-to-end). **control-plane:** re-registering a device (same key) now `UPDATE`s its stored `pq_ciphertext` instead of ignoring it — the rotation channel: a client re-encapsulates to the server's PQ key and re-registers with a fresh ciphertext (same tunnel IP). **oxide-serverd:** the peer-list poll detects a peer whose *derived PSK changed* since last applied and calls `replace_peer` (reconcile leaves live sessions untouched); pure `peers_with_rotated_psk` (existing+changed only, not new/unchanged) is unit-tested. **Mechanism is complete + verified at every layer testable here.** **Client driver DONE (2026-07-29):** `ConnectRequest.rekey_interval` (opt-in) drives a `rekey_loop` that runs *alongside* `run_tunnel` (via `tokio::select!`, cancelled when the tunnel ends) — it reads the `EngineHandle` through a `watch` channel wired into `on_ready`, and each interval re-encapsulates to the server's ML-KEM key → re-registers the fresh ciphertext (server derives the same PSK, applies on its next poll) → `replace_peer` locally (no reconnect, TUN stays up). `Resolved` now carries the server's PQ public key so the driver can re-encapsulate; `build_rekey_context` gates it to single-hop PQ (a peer already using a PSK) and is unit-tested. CLI: `oxide-client connect --rekey-secs N` (the agent passes `None` for now). **Server-first ordering (the live run's key lesson):** the first `netns-rekey-test.sh` run failed — the client swapped its PSK immediately, but the server only applies the new ciphertext on its next poll, so the client's handshake init hit a stale server, failed, and stalled ~5s on boringtun's retry timer; with a short interval the next rotation moved the target first and the watchdog tore the tunnel down. **Fix:** after re-registering, the client waits `REKEY_SERVER_GRACE` (3s) before swapping locally — the server applies the new PSK **first**, the client's current session keeps carrying traffic during the grace, and when it swaps its fresh handshake lands on a ready server (a ~1s blip, not a ~5s blackhole). `rekey_interval` > grace > server poll interval. **LIVE-VERIFIED** `scripts/netns-rekey-test.sh` (CP-mediated PQ tunnel, rekey 8s / poll 2s / grace 3s): tunnel up on the initial PSK, survives the rotations (14/18 pings), still up after, both sides log the swap — **PASS**. (The script also needed a client-ns default route for full-tunnel endpoint pinning + a sudo/rustup-robust build; it's the first netns harness running a real `serverd` with `[control_plane]`+PQ.) 174 tests.
- **2026-07-29 — 3B cross-platform: `oxide-net` facade + `net-macos` scaffold (verifiable-first).** Established the platform seam for a macOS/Windows data plane. New **`oxide-net`** facade re-exports the privileged network backend by build target (`#[cfg(target_os="linux")] pub use oxide_net_linux::…`; target-gated deps), so the **client** crates (client-core, oxide-agentd, oxide-client) depend only on `oxide-net` — a new OS is a new sibling wired in here, not an edit to every caller. Server-only pieces (NAT masquerade, `sysctl`) stay in `oxide-net-linux`, which `oxide-serverd` uses directly (servers run on Linux). New **`oxide-net-macos`** is `#![cfg(target_os="macos")]` (compiles empty on Linux, so it's a harmless workspace member) and mirrors the client contract. **Constraint:** this box is Linux — I can `cargo check --target x86_64-apple-darwin` (compile-check) but **cannot run** macOS binaries, so per the user's "verifiable-first" call: portable bits done for real (`bind_dual_stack` socket2, `shutdown_signal` tokio, `dns` resolv.conf), **utun** `TunDevice` implemented (PF_SYSTEM/SYSPROTO_CONTROL open, non-blocking `AsyncFd`, and the mandatory **4-byte AF header** utun prepends — stripped on recv, prepended on send via `writev`, so wg-core sees raw IP like on Linux) + `link_index` via `if_nametoindex`; **routing (BSD PF_ROUTE) + pf kill switch are stubbed** (`ErrorKind::Unsupported` + `TODO(macos)`) so it links/cross-compiles but a macOS client can't route until finished + live-verified on a Mac. **The cross-check earned its keep:** it caught a real `Send` violation — an `iovec` of raw pointers held across `.await`, breaking `TunQueue::send`'s `Send` bound — invisible to any Linux build; fixed by building the iovec inside the readiness closure. Known gap flagged: macOS names the device `utunN` (not `oxide0`), so client-core must use `tun.name()` there instead of the fixed `IFNAME` — a `TODO(macos)`. Linux workspace unchanged (170 tests, clippy `-D` clean on host + darwin target).
- **2026-07-29 — 2H observability finished (lean): /metrics enrichment + runtime DAITA/demux counters.** `/metrics` was only counts + per-server active_peers/capacity; the byte totals + feature config sat in the DB/admin-API only, and the privacy defenses had **no runtime counters**. Closed both, staying TUI/pure-Rust (no Grafana). **Phase 1 (CP-only):** `render_metrics` now emits per-server `oxide_server_{tx,rx}_bytes_total` (counter), `oxide_server_up` (gauge, from the staleness rule), and fleet adoption gauges `oxide_servers_{stealth,quic,daita,pq}` — all from one `servers` SELECT, no schema change. **Phase 2 (runtime, end-to-end):** wg-core `Shared` gained `daita_tx_real`/`daita_tx_cover`/`daita_rx_cover_dropped` (`Relaxed` atomics, incremented in `send_egress`/`shaper_loop` tag-byte-classify/`inbound_loop` — hot-path safe, `daita` crate stays pure) + promoted `decap_probes`; all four ride `EngineStats` → `HeartbeatRequest` (now `#[serde(default)]` + `ControlClient::heartbeat` takes a `&HeartbeatRequest` instead of a positional arg list) → serverd → 4 new `servers` columns → `/metrics` counters (`oxide_server_daita_tx_real_total`, `oxide_server_daita_rx_cover_dropped_total`, `oxide_server_decap_probes_total`) + `AdminServerInfo` → admin-TUI detail line (`daita: real ↑N drop ↓N`, `demux: N probes`). **Key decisions:** new counters stored **raw-latest** (not reset-safe like bytes — Prometheus handles per-restart resets via `rate()`; bytes stay reset-safe as billing-grade); `daita_tx_cover` is **client-only** (a server frames real + drops inbound cover → reports 0 cover sent, documented so a 0 isn't read as a bug); PQ/QUIC/stealth covered as **adoption gauges** (boringtun is opaque to a PQ handshake counter). **Deferred:** decoy/obfs runtime counters (Transport-layer seam), client-side DAITA counters in `oxide-tui`, persisting counter history. New wg-core loopback test `daita_counts_cover_and_real` (grab handles before `run()`); CP metrics/admin tests extended; live PG green (new columns migrate idempotently). 170 tests. **2H is now DONE.**
- **2026-07-27 — Per-server bandwidth graph + client latency auto-refresh.** Two follow-ons to the bandwidth/latency work: (1) **admin per-server sparkline** — the rate-differencing logic is now a reusable `RateHistory { tx, rx, prev }` (`record()` folds cumulative totals → per-refresh deltas); the App keeps `fleet_bw` (Overview) **and** a `server_bw: HashMap<id, RateHistory>` populated by `record_server_bandwidth()` (which **prunes** servers that leave the fleet), rendered as ↑/↓ sparklines in the Servers **detail pane** via a shared `bandwidth_row`. (2) **Client latency auto-refresh** — `refresh_servers` now folds in the `probe_latencies` sweep (so the list and its per-server ms refresh together), and `oxide-tui`'s run loop gained a **30s `interval_at` server tick** (offset one interval past startup) so latency stays current without pressing `p`. Cost note: the auto server-tick's probe can block the loop up to `tcp_ping`'s ~1.5s timeout when servers are unreachable (fast on a healthy fleet); `p` remains for an immediate re-probe. 169 tests.
- **2026-07-27 — Fleet-bandwidth-over-time sparkline (admin Overview).** The admin console's Overview tab now graphs fleet throughput. The control plane only stores *cumulative* per-server byte totals, so the TUI derives the rate **client-side**: each refresh, `App::record_bandwidth(tx_total, rx_total)` differences the new `AdminOverview` totals against the previous reading (first sample = baseline only; totals are monotonic so `saturating_sub` just guards the degenerate case) and pushes the per-refresh delta into a rolling in-memory history (`HISTORY_LEN=60`). `draw_fleet_bandwidth` renders ↑/↓ `sparkline()`s + the current per-second rate (`latest_rate` = latest delta ÷ `refresh_secs`); the graph is pinned to the bottom of the Overview under the stat cards, and shows "gathering samples…" until the second refresh lands. In-memory only (resets on restart) — no schema/CP change. Pure `record_bandwidth`/`sparkline`/`latest_rate` + render tests. 167 tests. **No Grafana/GUI** (user preference) — this is the in-TUI answer to fleet observability.
- **2026-07-27 — TUI quality-of-life batch (both clients).** Four commits on top of the admin/polish work, all verified without root (164 tests, clippy `-D` clean):
  1. **Client connect options + latency ranking** (`oxide-tui`): a **kill-switch toggle** (K) — connect now honors it (it always sent `false` before, a real gap); **live filter/search** (`/`) over id/country/city; **quick-connect-best** (b) → `Connect{server:None}`; and **latency ranking** — `p` runs a concurrent no-root **"TCP ping"** sweep (new pure `client-core::latency::tcp_ping`: times a `TcpStream::connect`; a *refused* port still counts as a round-trip, only a timeout is "unreachable"), shows per-server ms, and `t` sorts by latency vs load. Selection now runs over the filtered/sorted `App::visible()` list (all pure + unit-tested).
  2. **Admin drill-down + alerts + rotate-token** (`oxide-admin-tui`): a **detail pane** beside the fleet list (full metadata for the selected server); a pure `server_alert()` flagging **STALE** (no recent heartbeat) / **HIGH LOAD** (≥80% capacity) → colored row markers + an always-visible header **"N alerts" chip**; and **rotate-token from the TUI** (`R` arms a two-step confirm → `admin_rotate_token`, shows the new token + grace window).
  3. **Saved profiles** (new `oxide_common::profile`): both TUIs load `~/.config/oxide/{client,admin}.toml` (honoring `XDG_CONFIG_HOME`) so common flags/tokens aren't retyped; **flags override the file**, a value absent from both is a clear startup error, a missing default file is fine, a malformed or missing-`--config` file is a hard error. `load`/`load_named` unit-tested (present/missing/malformed/explicit-missing).
  - **Design notes:** latency is a no-root *estimate* (TCP-connect timing) for *ranking*, not a precise path RTT — documented as such. The admin rotate action is destructive, so it's gated behind an in-TUI confirm. Profile parsing is strict (typos error) but absence is lenient (flags-only still works).
- **2026-07-27 — Admin console + client-TUI polish, on real byte accounting (user pivot: "cool TUIs").** Three cohesive additions, all verified without root (148 tests, clippy `-D` clean, live PG green):
  1. **Real bandwidth accounting (data plane → CP → admin API).** The server's heartbeat now carries the engine's **cumulative** `tx_bytes`/`rx_bytes` (`common::api::HeartbeatRequest` + `control-client::heartbeat` gained two args; `oxide-serverd`'s heartbeat loop passes `stats.tx_bytes/rx_bytes`). The control plane folds each reading into **monotonic, reset-safe** per-server totals via the pure `accumulate_bytes(total, last, reported) -> (new_total, new_last)` (delta = `reported - last`, or the whole `reported` when it went *down* = server restart; unit-tested for first-report/growth/reset/idempotent-repeat). New `servers` columns `tx_bytes_total`/`rx_bytes_total`/`last_tx_bytes`/`last_rx_bytes` (SQLite CREATE + idempotent ALTER; PG CREATE + `ADD COLUMN IF NOT EXISTS`). **No-logs note:** these are *fleet-level, per-server* aggregates (like `active_peers`), never per-account traffic — the posture holds.
  2. **Admin read API** (admin-token-gated, same bearer as `provision`/`rotate-token`): `GET /v1/admin/servers` → `AdminServerInfo[]` (endpoint, location, capacity/active_peers, healthy, transport, daita/pq/stealth flags, byte totals, `last_heartbeat_secs`, `created_at`) and `GET /v1/admin/overview` → `AdminOverview` (fleet counts, healthy count, summed peers + bytes, feature-adoption counts). `control-client::admin_list_servers`/`admin_overview`. In-process integration test drives two servers through byte-growth + a reset and asserts totals + aggregates.
  3. **`oxide-admin-tui`** (new crate) — a **read-only operator console**: tabs Overview (stat cards incl. **estimated cost** from a transparent model — `CostModel { per_gb, per_server_hour }`, egress-billed, `--cost-per-gb`/`--cost-per-server-hour`), Servers (load bars + colored feature badges + per-server cost), Features (adoption gauges). Pure helpers (cost/adoption/load/`hours_since`) unit-tested; **headless `TestBackend` render tests** for all three tabs.
  4. **`oxide-tui` polish** — the client TUI is now phase-aware and lively. `TunnelStatus` gained `phase: ConnPhase` (`disconnected|selecting|connecting|connected|reconnecting`, derived by the agent from the `ConnEvent` stream — the agent tracks a `phase` cell + promotes `connecting`→`connected` once a handshake lands), plus `handshake_age_secs`, `transport`, `daita`, `kill_switch` (all `#[serde(default)]`, back-compat). `ConnInfo` carries `transport`/`daita`; `TransportKind::label()`. The header shows a **braille spinner** while working, a **link-health dot** (live/stale/unknown from handshake age), a **capability badge row** (transport/DAITA/PQ/STEALTH/KILL, bright=on), and **live throughput sparklines** (`sparkline()` over per-poll byte deltas; a 120ms anim tick keeps the spinner smooth). Pure helpers (`sparkline`, `link_health`, delta recording) + render smoke tests. **No live-root path needed** — the agent/TUI wiring is covered by unit + render tests; the netns harnesses still exercise the underlying connect.
- **2026-07-24 — 2F (partial): kill switch over netlink via `rustables`; NAT stays on `nft`.** Chose **`rustables`** (pure-Rust nftables-over-netlink, no runtime C dep) after confirming the alternatives: `nftnl` needs libnftnl (C dep); the `nftables` crate still shells out to `nft`. **Kill switch** (`net-linux/src/killswitch.rs`) is now built entirely over netlink: a pure `permits(server_ip, port, tun_if) -> Vec<Permit>` describe layer (unit-tested — loopback + tunnel iface + the WG server endpoint) feeds `apply()`, which builds the `inet oxide-ks` table + `output` chain (`ChainType::Filter`, `HookClass::Out`, `ChainPolicy::Drop`) + one `accept` rule per permit (`oiface`/`daddr`+`dport(UDP)`) and sends it as one `Batch`. `disable()` sends a `Del` of the table and ignores ENOENT (idempotent). No more `nft` shell-out for the kill switch (which runs on **clients**). **NAT (`nat.rs`) deliberately stays on `nft`:** its forward-chain **MSS clamp** (`tcp option maxseg size set rt mtu`) is an `exthdr`-mangle statement `rustables` 0.8 has no expression for; dropping it reopens the "ping works, curl hangs" PMTU black hole. Full NAT-via-netlink would need `nftnl`/libnftnl (a C dependency) — deferred by the pure-Rust choice; NAT runs on servers where `nft` is present anyway. **Build note:** `rustables` pulls `bindgen`, so building `net-linux` now needs **libclang at build time** (no runtime C dependency; the binary stays self-contained). The rustables `apply` can't be unit-tested (needs root + netfilter) — its live test is `netns-fulltunnel-test.sh` (kill switch enabled → underlay leak blocked), now exercising the netlink path; **re-run it to bank the live pass**. 127 tests.
- **2026-07-23 — 2G WireGuard-over-IPv6 (dual-stack underlay).** The tunnel's UDP underlay can now bind and serve over IPv6 (previously IPv4-only; inside-tunnel v6 already worked). New `net-linux::udp::bind_dual_stack(port)` builds a `socket2` UDP socket on `[::]:port` with **`IPV6_V6ONLY` cleared**, so one socket serves both v4 (as IPv4-mapped) and v6 peers; if the `[::]` bind fails (IPv6 disabled) it **falls back to `0.0.0.0:port`**. Both `oxide-serverd::build_server_transport` and `client-core::build_client_transport` now bind through it (returns a nonblocking `std::net::UdpSocket` → `tokio::UdpSocket::from_std`). A client can therefore dial a v4 **or** v6 `endpoint` from one socket. `control-plane::host_of` already parsed bracketed `[2001:db8::1]:51820` (rsplit on `:` + trim `[]`) — locked in with a test. Route pinning is now **family-aware**: new `netlink::default_route_family(v6)` (runs `ip -6 route show default`) + `client-core::default_gw_for(ip)` resolve the gateway of the **endpoint's/exclude's own family**, so a v6 full-tunnel endpoint pins via the v6 default (a v4 gateway would be a family mismatch). Verified without root: `udp` dual-stack socket test (one socket receives from a v4 and a v6 client), a v6-underlay loopback handshake in `wg-core/tests/tunnel.rs` (`[::1]` sockets, inner v4 packet), and the `host_of` v6 test; live `scripts/netns-ipv6-test.sh` (v6-only veth, client dials `[fd00:50::1]:51820`, inner v4 ping through the tunnel). Deferred: control-plane **v6 tunnel-IP allocation** (assigning v6 addresses) — not needed since inside-tunnel v6 works via static config; the mimic (TCP) transport still binds v4 (a small follow-up). New dep: `socket2 = 0.5` in `net-linux`. 127 tests.
- **2026-07-23 — Bugfix: netlink errors collapsed to `ErrorKind::Other` (found via the live 3D netns run).** `net-linux::netlink::to_io` flattened **every** rtnetlink error to `io::Error::other(string)`, so the errno/`ErrorKind` was lost. Effect: 3D split-include's tolerate-`AlreadyExists` check (re-adding the interface's own subnet route, which the kernel auto-installs on address assignment) never matched — the `EEXIST` propagated and **aborted the connect** (`installing split-include tunnel route: File exists (os error 17)`). Fix: `to_io` now special-cases `rtnetlink::Error::NetlinkError(msg)` → `msg.to_io()` (`io::Error::from_raw_os_error(errno.abs())`), so `EEXIST`→`AlreadyExists`, `ENOENT`→`NotFound`, etc. — correct kinds for every netlink call, not just this one. Invisible to unit tests (rtnetlink `ErrorMessage` is `#[non_exhaustive]`, can't be built off a real socket); the live netns path is its test. **After the fix, `netns-classic-test.sh` PASSES live** (3D auto-include: `10.60.0.0/24` via oxide0 with no manual route + full exit/NAT). Also fixed `netns-newidentity-test.sh`: it greps the tracing `server=` **field**, which tracing wraps in **ANSI colour codes**, so a literal match found nothing — the exit-id extractor now strips ANSI (`sed -E 's/\x1b\[[0-9;]*m//g'`) first (message-text greps like the reconnect test were unaffected because only fields are coloured). **3E verified live** from the run's own log: `connecting server=ghost-a` → SIGUSR1 → `rotated device key` → `connecting server=ghost-b` (key rotated + exit switched). **Live status: 2E/3D/3E + fulltunnel/reconnect all green.**
- **2026-07-23 — 3E "new identity" (ephemeral key + exit rotation).** One action → a fresh device key **and** reconnect to a *different* exit, for Tor-style per-session unlinkability. New `client-core::rotate_device_key` (generates a key, writes a sibling temp file, `set 0600`, then **atomically renames** over the key file — a crash never truncates it) and `rotate_identity(key_file, current_server) -> (SecretKey, Vec<String>)` (rotates + returns the exclude set = just the exit we're leaving, so repeated switches can't exhaust the pool). `run_supervised` gained a **new-identity generation channel** (`watch::Receiver<u64>` alongside `stop`): the per-attempt teardown future resolves on *either* stop or a generation bump, and afterward the two are told apart by re-reading `stop`/the generation — a bump rotates the key, sets `identity_exclude`, and loops to reselect. New `ConnEvent::NewIdentity`; the exclude assembly at the loop top now unions the failed-cooldown set with `identity_exclude`. Triggers: **agent** `AgentRequest::NewIdentity` (bumps the active connection's `new_id` sender via `send_modify`) surfaced as the **TUI `n` keybind**; the **CLI `connect`** rotates on **SIGUSR1** (`kill -USR1 <pid>`). Mesh keeps its device key (new identity is scoped to exit connections). Verified: pure `rotate_identity` unit test (different pubkey, persisted, 0600, empty exclude when never-connected) + agent-protocol roundtrip; live `scripts/netns-newidentity-test.sh` (two ghost exits, auto-select, SIGUSR1 → asserts the key rotated and the exit switched, entire run kept under `connect_timeout`=20s so only the action — not a natural link-dead failover — can switch the exit). **Caller signature change:** `run_supervised` takes the `new_identity` receiver before `on_ready` — both call sites (`oxide-agentd`, `oxide-client`) updated. 124 tests.
- **2026-07-23 — 3D split tunnelling (per-destination).** The client now routes per-CIDR instead of only full-tunnel-or-nothing. New `[interface] split_include` / `split_exclude` (`Vec<IpNet>`, `#[serde(default)]`, back-compat). A **pure** `client-core::split::plan_routes(allowed_ips, include, exclude) -> RoutePlan` decides, **per address family**: full-tunnel (a `0.0.0.0/0`/`::/0` peer swings that family's default), **include** (each specific `allowed_ips` CIDR + any `split_include` is routed on-link **via oxide0** — this removes the manual `ip route add <cidr> dev oxide0` the netns harness used to need), and **exclude** (`split_exclude` CIDRs pinned **around** the tunnel via the original default gateway; a more specific exclude outranks the tunnel default by longest-prefix). `run_tunnel` consumes the plan, resolves the original gateway once (only if full-tunnel or excludes exist), installs routes, and **tracks every added route** (`tunnel_routes` + `gateway_pins`) so teardown removes exactly them. New netlink `add_route_via`/`del_route_via` (arbitrary-prefix via-gateway; `host_route_via` now delegates to the general `net_route_via`). **Kill switch is refused when `split_exclude` is set** (checked before any netlink mutation) — excluded CIDRs must reach the underlay, which the KS blocks. **CP/mesh connect paths stay full-tunnel** (their synthesized `InterfaceConfig` sets empty split lists); split is driven by the static `up` config for now. Verified: 6 pure `plan_routes` unit tests + a config round-trip test; live `scripts/netns-split-test.sh` (full-tunnel + `split_exclude`, asserts the FIB via `ip route get`: public IP→oxide0, excluded CIDR→original gw, endpoint pinned) and `netns-classic-test.sh` now asserts the auto-include (`ip route get 10.60.0.5` is `dev oxide0`, no manual step). Per-**app** split (cgroup/fwmark) is a separate v2. 122 tests.
- **2026-07-23 — 2E receiver-index peer demux.** The inbound demux (`wg-core` `engine.rs::handle_incoming`) no longer falls back to scanning **every** peer's `decapsulate` for an established session. A WireGuard **data** message (type 4) carries, at bytes `[4..8)` LE, the **receiver index we assigned** to that peer's session; new pure `parse_recv_index` extracts it (type-4-only, length-guarded), and a `Shared.recv_index_to_peer: Mutex<HashMap<u32, PeerId>>` cache maps it straight to the peer. Resolution order: **index → source-addr cache → full scan**. The index also **survives roaming** (unlike the source address), and stale entries from a superseded session are never hit again (a rekey assigns a fresh index; old ones age out, pruned on `remove_peer`). Handshakes (types 1/2/3) and a session's first packet still pay the one-time scan that learns the mapping. Verified: `parse_recv_index` unit tests + an integration test that pads the server with **40 decoy peers** and asserts steady-state traffic costs ~1 `decapsulate` probe/packet (a `decap_probes` atomic counter on `Shared`, exposed via `EngineHandle::decap_probes` behind `cfg(test|test-util)`), not O(peers). **Invariant unchanged:** still never `.await` while the `Tunn` mutex is held. 115 tests.
- **2026-07-23 — DNS resolvectl path: fallback hardening + live check.** `dns::set_dns` now **falls back to `/etc/resolv.conf`** if the systemd-resolved path fails (resolvectl missing/errors, or the interface isn't visible to resolved — e.g. in a netns) instead of failing the tunnel. New `scripts/resolvectl-check.sh` verifies the exact resolvectl commands (`resolvectl dns <if> <ip>` + `~.` routing domain + `revert`) on a throwaway dummy interface — so the resolved backend is verifiable on a resolved host without a full tunnel or touching real DNS. (This dev box has no systemd-resolved, so the resolved path is exercised via that script after `systemctl start systemd-resolved`; the resolv.conf fallback is what actually runs here and is unit-tested.)
- **2026-07-23 — Server-token rotation + versioned API (2C lifecycle).** **Token rotation:** rotate a server's control-plane auth token with a **grace window** — the previous token keeps working for `TOKEN_ROTATION_GRACE_SECS` (1h) so the operator can update the server config + reload with no session drop. New `servers.prev_auth_token`/`prev_token_expires_at` columns (idempotent), `auth_server` accepts current-or-(previous-within-grace), admin endpoint `POST /v1/admin/servers/:id/rotate-token` + `control-client::admin_rotate_token` + a local `oxide-control-plane rotate-token --id` CLI (shared `rotate_server_token`). Test proves both tokens work during grace and a token *two* rotations old is rejected. **Versioned API:** `GET /version` (unauthenticated) → `{api, server, git_commit}` (control-plane `build.rs` bakes the commit); shared `common::api::API_VERSION` (`v1`); `control-client::version()`/`is_compatible()`. Policy documented: `/v1` is stable and **additive-only** (new optional `#[serde(default)]` fields + new endpoints); a breaking change ships as `/v2` alongside `/v1`. 111 tests.
- **2026-07-23 — Bugfix: reconnect leaked the TUN / engine tasks (found via live reconnect test).** `Engine::run` spawned **detached** data-plane tasks (outbound/inbound/timer, + limiter/shaper). When a supervisor tore the tunnel down on link death (`run_tunnel`'s outer `select!` drops `engine.run()`), those tasks kept running and kept holding `Arc<Shared>` — so the TUN device was never released, the tasks leaked, and the *next* reconnect attempt failed with `bringing up tun interface: Device or resource busy`. Fixed: `run` registers every spawned task with an `AbortGuard` that aborts them when `run` returns **or is cancelled**, releasing `Shared`/the TUN promptly; and `bring_up_interface` now retries `TunDevice::create` briefly on `EBUSY` to cover the async-abort window. Live reconnect harness (`netns-reconnect-test.sh`) now PASSES — the always-on supervisor loops with backoff across multiple attempts. 109 tests.
  - This was invisible to unit tests (a single connect never re-creates the interface); only the always-on reconnect loop under root surfaced it. Third real bug the live pass caught, after the kill-switch data-plane crash and the `Some("")`/NULL column read.
- **2026-07-23 — Bugfix: NULL optional DB columns read as `Some("")` (found via live reconnect test).** The 2B dialect refactor made `DbRow::opt_text` decode as `String`, which turns a SQL `NULL` into `Some("")` on SQLite (the old code decoded `Option<String>` → `None`). Effect: a server registered without a PQ key exposed `pq_public_key: Some("")`, so the client attempted a post-quantum handshake against an empty key and **failed to resolve** — i.e. **every non-PQ server broke client connects** (also mis-read empty obfs/transport). Unit tests missed it (they always set the fields they check); the netns reconnect harness caught it. Fixed: `opt_text` decodes `Option<String>` (NULL → None) and treats `""` as absent; regression test `unset_optional_server_fields_are_none_not_empty_string`. 109 tests.
- **2026-07-23 — Live verification pass + data-plane resilience fix.** Drove the classic path end-to-end under root in network namespaces (new `scripts/netns-classic-test.sh` = tunnel + NAT exit-IP-hidden; `scripts/netns-fulltunnel-test.sh` = full-tunnel `0.0.0.0/1`+`128.0.0.0/1` override + kill switch blocks underlay leaks). Both **PASS live**. The full-tunnel run surfaced a real bug: `wg-core` `outbound_loop` **propagated** a UDP send error (an nft/kill-switch drop → `sendto` EPERM), tearing down the whole engine. Fixed: it now drops the one packet and continues (like the other loops) — the data plane must ride through a firewall blip / route flap / `ENOBUFS`, especially under the kill switch and reconnect. 108 tests.
  - Note (documented): "server died mid-session" liveness detection is bounded by WireGuard's ~120 s rekey, so `stale_after`=150 s; the fast path is `connect_timeout`=20 s (never-connected → re-select). A multi-homed server that replies from a non-endpoint IP can desync WireGuard's learned endpoint from the kill-switch-permitted IP (a test-topology artifact; real single-IP servers are fine) — a future hardening is binding the server socket to its endpoint / using `IP_PKTINFO`.
- **2026-07-23 — One-command provisioning (2D, classic-VPN track).** Stand up a server without shelling into the control-plane host: `oxide-serverd provision --control-plane <url> --admin-token <t> --id <id> --endpoint <host:port> [--transport quic --daita --pq --nat-egress auto ...]` generates the WG (and optional PQ / obfs) keys, **registers the server remotely** via a new admin API, and writes a ready-to-run `server.toml`; then `sudo oxide-serverd up`. New `POST /v1/admin/servers` (admin-bearer-gated; `AppState::with_admin_token`, `oxide-control-plane serve --admin-token`/`OXIDE_ADMIN_TOKEN`) reuses `add_server`; `control-client::admin_add_server`; `common::api::AdminAddServer{Request,Response}`; `AppError::Forbidden`. The config renderer (`render_server_config`) is pure + unit-tested (parses back to a valid `Config`); the admin endpoint is in-process tested (correct token registers + is selectable with its transport metadata; wrong token / admin-disabled → 403). Verified live end-to-end without root: `provision` against a running control plane produced a real token + a valid quic+daita+pq config. 108 tests.
  - Decision: **admin token gates the whole endpoint** (disabled = 403, not just wrong-token) so the provisioning surface is invisible unless explicitly enabled. The `provision` config part needs no root (keygen + HTTP + file write); only `up` (TUN/NAT) does — matching the "CAP_NET_ADMIN non-root deploy" goal. `--nat-egress auto` writes a `[nat]` with no `egress` so the daemon auto-detects.
- **2026-07-23 — DNS that holds on modern Linux (2F, classic-VPN track).** The old direct `/etc/resolv.conf` rewrite was clobbered by `systemd-resolved` on most desktops — a real DNS **leak**. `net-linux::dns` now picks a backend: **systemd-resolved** (when its stub `/run/systemd/resolve/stub-resolv.conf` is present) drives per-interface DNS via `resolvectl dns oxide0 <ip>` + a `~.` **routing domain** (`resolvectl domain oxide0 ~.`) so resolved sends *every* query to the tunnel DNS (no leak to the LAN resolver), reverted with `resolvectl revert oxide0`; else the **resolv.conf** direct-rewrite fallback (unchanged). `set_dns` now takes the interface; `DnsGuard` is an enum (revert-link vs restore-file). The backend choice, the `resolvectl` arg vectors, and the file body are pure + unit-tested (4 dns tests); applying them is root/netns-tested. 104 tests.
  - Decision: **`resolvectl` CLI, not the resolved D-Bus API** — matches wg-quick, and net-linux already shells out for nft/sysctl. A NetworkManager-manages-DNS-itself deployment (no resolved) can still reassert on a network change — a NM-D-Bus backend is the documented follow-on. Note: with the reconnect loop, DNS is set/reverted per tunnel attempt (a momentary revert during a reconnect gap; the kill switch covers traffic meanwhile).
- **2026-07-23 — Reliability: always-on auto-reconnect + failover (classic-VPN track).** A dropped tunnel now recovers instead of staying dead. New `client-core::reconnect` (pure, unit-tested): `ReconnectPolicy`, a liveness decision `link_dead()` over the freshest WireGuard handshake age (new `EngineStats.handshake_age_secs`), capped `backoff()`, and `select_best()` (client-side least-loaded pick that avoids a just-failed server). `run_tunnel` gained an optional liveness **watchdog** (`policy: Option<ReconnectPolicy>`) and returns a `TunnelOutcome` (`Disconnected` | `LinkDead{was_up}`); `run_supervised()` is the loop: resolve → tunnel → on link-death re-select a **different** server (cooldown on the dead one) and reconnect, backing off, until a `watch`-channel `stop` fires. Wired into `oxide-client connect` (always-on by default) **and** `oxide-agentd` (shared `meta`/`handle` cells updated on each reconnect, so TUI/GUI status follows the current server). Verified without root: 5 `reconnect` unit tests (fresh=alive, connect-timeout, established-then-stale=dead, backoff caps, failover selection). 102 tests. Live reconnect path is netns/root-verified.
  - Decision: **liveness = handshake age**, not rx-byte stalls — WireGuard rekeys ~every 2 min, so an age growing past `stale_after` (150 s) unambiguously means the peer is gone; `connect_timeout` (20 s) catches "never connected." `was_up` distinguishes an established drop (retry fast) from a failed connect (back off).
  - Decision: **watchdog is opt-in (`None` for mesh/static `up`)** — a peerless mesh is legitimate and static `up` has nothing to fail over to, so only `run_supervised` (CP connect) arms it. Failover re-selects **client-side** (`select_best`, excluding the dead server for a cooldown) since a just-died server's control-plane heartbeat stays "healthy" for up to 90 s.
  - Decision: **`EngineHandle` Clone hand-written** (not derived) so cloning doesn't demand `T: Clone` — the watchdog + agent both need a second handle.
- **2026-07-22 — Transport choice distributed via control plane (Sprint 2A-2).** Finishes 2A: the server's transport (`plain|obfs|quic|mimic`) and `daita` flag are now stored per-server and handed to clients at registration, like the obfs key — so a control-plane `oxide-client connect` builds the **matching** transport instead of only obfs/plain. New `servers.transport`/`servers.daita` columns (idempotent `ADD COLUMN`/migration on both backends), `add_server`/`NewServer` + `oxide-control-plane add-server --transport <kind> --daita` flags, `RegisterDeviceResponse.transport`/`.daita` DTO fields, and `client-core::resolve_registration` maps them into `InterfaceConfig` (unknown/absent → the back-compat `transport_kind()` rule). For multihop this is the **exit's** transport (tunnel terminates there), consistent with the obfs key. Verified: an SQLite API test (register against a quic+daita server → response carries `transport="quic"`, `daita=true`, idempotent re-reg too) + a client-core test (`resolve_registration` → `iface.transport_kind()==Quic`, `iface.daita`) + the live-PG flow still green after the column migration. 97 tests.
  - Now a CP-selected connect to a QUIC/DAITA/mimic server works end-to-end (the 2A builders were already wired); previously CP connects were obfs/plain-only. `decoy_backend` stays server-only (not distributed — clients don't need it).
- **2026-07-22 — Concurrency-safe control plane / multi-node ready (Sprint 2C).** Removed the single in-process `reg_lock` that serialized all registration (the 2B bottleneck), so **multiple API nodes can share one Postgres**. Correctness now comes from the **database**: `CREATE UNIQUE INDEX IF NOT EXISTS` on `devices(server_id, tunnel_ip)` and `relays(entry_id, listen_port)` (idempotent, so existing DBs pick them up), plus `db::is_unique_violation()` (portable across SQLite/PG). Device registration, mesh registration, and relay-port allocation became **lock-free retry loops**: compute a candidate, insert, and on a UNIQUE violation re-read (idempotent → return the winner's row) or re-pick the next free slot (`ALLOC_RETRIES` = 16). SQLite gets **WAL + a 5s busy_timeout** so concurrent writers proceed instead of `SQLITE_BUSY`. Verified: a 10-way concurrent-registration test asserts distinct IPs on **both** SQLite and live Postgres. 96 tests.
  - Decision: **DB-side uniqueness + retry, not an app lock or a Postgres advisory lock** — it's portable (same code on SQLite and PG), needs no cross-node coordination, and turns the race into a cheap retry. The idempotent re-check inside the loop also cleanly handles the concurrent *same-device* case (public_key UNIQUE) — the loser returns the winner's IP.
  - Decision: **`CREATE UNIQUE INDEX IF NOT EXISTS`, not an inline table `UNIQUE`** — inline constraints don't retrofit onto tables already created by `CREATE TABLE IF NOT EXISTS` (the exact bug that first made the live-PG concurrency test assign a duplicate IP). A pre-existing DB with duplicates fails index creation loudly on PG (surfaces the problem) / is ignored on SQLite (dev tolerance).
  - Known 2C remainder (not blocking multi-node reads/writes): a concurrent *schema-init* race if two nodes start at the exact same instant (`CREATE TABLE IF NOT EXISTS` is racy in PG — stagger starts or add an init retry); server-token rotation with grace; client re-selection on server death; versioned zero-downtime API.
- **2026-07-22 — Postgres backend (Sprint 2B).** The control plane now runs on **SQLite (default) or Postgres**, selected by the connection string (`postgres://…` → Postgres, else SQLite path). sqlx is compile-time DB-typed, so instead of a generic-over-`Database` rewrite we added a small **dialect layer** in `db.rs`: `Db{Sqlite(SqlitePool)|Postgres(PgPool)}` wraps the pool; `DbRow{Sqlite|Postgres}` exposes typed getters (`text`/`opt_text`/`int`/`opt_int`) that dispatch `try_get` (impl'd for both row types); `Val` is a bind param; the query methods rewrite `?`→`$n` for Postgres. **All SQL is written once** (SQLite `?` style); only the schema DDL branches (Postgres uses `BIGINT`/`BIGSERIAL` so `try_get::<i64>` decodes uniformly; SQLite keeps `INTEGER`/`AUTOINCREMENT` + its idempotent `ALTER` migrations). ~20 call sites converted to `pool.execute/fetch_optional/fetch_all/scalar_*`. Verified: existing SQLite tests unchanged (**no regression**) + a new `tests/postgres.rs` runs the full account/device/metrics flow against **live Postgres 18.4** (gated on `OXIDE_TEST_PG_URL`, skips cleanly in CI; unique server id per run). 95 tests.
  - Decision: **dialect enum over the sqlx `Any` driver** (user's call) — `Any` doesn't rewrite `?` placeholders and has type-mapping caveats; the enum keeps SQLite fully tested as one concrete branch and the Postgres branch is now proven against a real server, not guessed.
  - Decision: **`try_get` + `unwrap_or_default`/`.ok()` in `DbRow`** rather than panicking `row.get` — a schema/type mismatch degrades to a default/`None` in a request handler instead of a 500-with-panic. Postgres integer columns are all `BIGINT` so `i64` decode never type-mismatches.
  - Run Postgres: `oxide-control-plane --db 'postgres://user@host/oxide_cp' serve …`. The `reg_lock` still serializes IP allocation (correctness holds on either backend); a Postgres deployment can now run multiple API nodes once that lock becomes advisory/DB-side (future).
- **2026-07-22 — Verifiable no-logs (Sprint 1D-1/1D-2/1D-3).** Turns the no-logs posture from a promise into something enforced + provable. Two new pure crates + serverd wiring. Verified without root: 15 new tests (2 seccomp + 13 attest). 94 tests total, clippy + fmt clean.
  - **1D-1 enforced no-logs** (`oxide-seccomp`): a seccomp-BPF filter that makes the process **unable to create or write files**. `oxide-serverd` applies it (config `[hardening] no_disk_writes = true`) *after* setup, right before the packet loop, across all tokio threads (`apply_filter_all_threads`, inherited by children). **Key insight:** seccomp can't tell a file fd from a socket fd (no pointer deref), so we gate at `openat`/`open` **flags** (deny any write/create-intent open) + the FS-mutating syscalls (`creat`, `unlinkat`, `renameat2`, `truncate`, `openat2`, …) — *not* the `write` syscall, which sockets and stdout logging also use. Denials return `EPERM` (safe degrade; `KillProcess` is a one-liner for a stricter posture). Test forks a child, applies the filter, proves a file create is refused with `EPERM` while a socket + read-only open still work — **no root needed** (`PR_SET_NO_NEW_PRIVS`).
  - **1D-2 signed build manifest** (`oxide-attest`): an Ed25519 **release key** signs a `BuildManifest` (binary SHA-256 from `/proc/self/exe` + git commit + version + build time) → `SignedManifest`; a client pinning the release public key verifies it and can refuse unknown/tampered builds. Signature is over a fixed **canonical** encoding (not JSON). `oxide-serverd attest-genkey` (release keypair) + `oxide-serverd manifest --key <seed>` (emit the signed manifest for the running binary); a `build.rs` bakes in the git commit + `SOURCE_DATE_EPOCH`-aware build time. Verification is always against the **pinned** key, never the one embedded in the manifest — so an attacker who re-signs a tampered manifest with their own key still fails.
  - **1D-3 transparency log** (`oxide-attest`): an append-only, **hash-chained** log of deployed builds (`LogEntry{seq,binary_sha256,git_commit,prev_hash}`), with a signed head (`SignedHead` over `head_hash‖count`). `verify_log(entries, head, pinned_key)` checks chain integrity + head signature; editing any past entry breaks every later hash and the head. A client proves its server runs a build that was *publicly logged*, not one swapped in just for it.
  - **Decision: filter `openat` flags, not `write`** — the only enforceable way to say "no disk writes" in seccomp (it's fd-blind). Apply post-setup because the daemon legitimately opens `/dev/net/tun` `O_RDWR` at startup. Caveat: nft teardown (`disable_masquerade`) execs `nft` under the inherited filter — fine for `delete table` (netlink, no file writes); documented that `no_disk_writes` pairs best with a static config.
  - **Remaining (1D wiring):** distribute the signed manifest + transparency log via the control plane so the client verifies **at connect** (overlaps 2A-2 CP distribution); reproducible-build script; `1D-4` HW attestation (TPM/SGX) is out of scope (needs hardware — flagged to user).
- **2026-07-21 — Transport selector (Sprint 2A, local config).** Config now selects the wire transport instead of only obfs-on/off. New `common::TransportKind` (`plain|obfs|quic|mimic`) + `interface.transport` / `interface.daita` fields; `InterfaceConfig::transport_kind()` applies the **back-compat rule** (an `obfuscation_key` with no `transport` still means `obfs`). Both daemons build the selected transport from config: `oxide-serverd::build_server_transport` (binds/listens; `quic` uses `decoy_backend`; DAITA → `Daita::server()`) and `client-core::build_client_transport` (client role; `mimic` connects to the peer endpoint; DAITA → `Daita::client()`). `Config::validate` rejects `daita` without stealth and a stealth transport without a key. Verified without root: 3 config tests (back-compat/explicit parse, daita-needs-stealth, stealth-needs-key). 83 tests, clippy + fmt clean. **Scope:** local config only — control-plane distribution of the choice (like the obfs key) is the deferred 2A-2 follow-up.
  - Decision: **`transport` enum + orthogonal `daita` bool**, not a single 5-valued selector — DAITA isn't a transport, it *layers* on a stealth one (`Engine::with_daita`). Keeps the model correct.
  - Decision: **builders live in the daemons, not `wg-core`** — preserves the "wg-core knows nothing about config" invariant; the small role difference (server binds/decoy; client connects for mimic) is genuine, so minor duplication beats importing config types into the engine.
- **2026-07-21 — Undetectable server: decoy-forwarding (Sprint 1B).** New `wg-core::decoy::DecoyForwarder` + `Transport::quic_mimic_with_decoy`. On the QUIC-mimicry transport, an unauthenticated first-contact Initial (forged/stale/replayed — the active-probe vector) is no longer silently dropped: it's **spliced to a configured decoy backend** (a real TLS/QUIC endpoint), whose response is relayed back to the prober **through the server's own listen socket** — so the port answers like an ordinary QUIC/HTTP-3 server, not a dead port. A source classified as a prober stays spliced for the flow's lifetime (idle-reaped). Reuses the `relay` per-source-upstream pattern. New `interface.decoy_backend` config (`host:port`). Verified without root: 2 transport tests (forged probe answered by a stub backend from the server's port; genuine Initial still tunnels *concurrently* with a decoyed probe). 80 tests at 1B, clippy + fmt clean.
  - Decision: **trigger = failed QUIC Initial only**, not unknown short-header/garbage. Real QUIC servers also drop unassociated packets silently, so that's already realistic; and it preserves WireGuard endpoint **roaming** (a roaming client sends short-headers from a new address — those must still reach the tunnel, gated by the WG AEAD). The distinguishing active probe is a crafted Initial, which is exactly what we decoy.
  - Decision: **QUIC path only for v1.** TLS-mimic (TCP) decoy is a documented follow-up; serverd wiring of quic-with-decoy arrives via the transport selector (2A). Threat model: strongest when `decoy_backend` is a real service you host; our mimicry Initial isn't byte-perfect QUIC so a strict backend may send an error response (still *a* response); a global observer could compare authenticated vs decoyed timing.
- **2026-07-21 — DAITA v1: traffic-analysis defense (Sprint 1A).** New pure `daita` crate (cell codec + `Shaper`) + an engine-level queue in `wg-core` (`Engine::with_daita`, `crate::daita::Daita`). Finishes the traffic-analysis story obfs started (size buckets) by adding the **rate** and **cover** dimensions: the client drains outbound WG datagrams through a bounded queue at a fixed **slot** cadence, emitting exactly one fixed-size **cell** per slot — a `REAL` cell (a queued datagram) or a `COVER` cell (droppable filler) — so a passive/ML observer or our own multihop entry sees only steady, contentless, constant-size volume. A 1-byte REAL/COVER tag lives *inside* the obfs frame, so cover is recognized and dropped **before boringtun**; a fixed cell size means the obfs layer always picks the same size bucket (one wire size for every cell). v1 shapes **client→server egress, single peer**; the server runs `Daita::framing` (wraps replies as cells so the client can parse, drops inbound cover) but doesn't generate cover/constant-rate — server→client is size-normalized but reactive. Verified without root: 10 `daita` unit tests (roundtrip, one-size real==cover, cover-when-idle, FIFO drain, drop-oldest-when-full, malformed/oversize rejection) + capstone `tunnel_works_with_daita_shaping` (a real WG tunnel over obfs with DAITA on: a packet crosses **and** the steady cover stream never leaks to the server's tunnel). 78 tests, clippy + fmt clean.
  - Decision: **engine-level queue, not a `Transport` wrapper** (Part-4 fork #3) — keeps the `Transport` enum and the peer-generic engine clean; the shaper is a fourth detached task (`shaper_loop`) alongside outbound/inbound/timers, and all four client egress sites (`init_handshakes`, `outbound_loop`, `handle_incoming` replies, `timer_loop` keepalives) funnel through one `send_egress` helper so **every** client→server datagram is shaped → truly constant rate.
  - Decision: **framing is bidirectional, shaping is client-only** — the type byte changes the payload format, so both ends must speak the cell codec (the server strips the tag before decapsulate); only the client runs the constant-rate shaper + cover. Bidirectional/per-hop rate shaping is Sprint 4A.
  - Decision: **DAITA implies stealth** (fork #4) — cells ride inside the obfs frame (need the shared key to drop cover), so a stealth transport (obfs/QUIC/TLS-mimic) must be on. `with_daita` uses `Arc::get_mut` so it's additive (no `build` signature change) but must be called before `handle()`/`run()`.
  - Cost (documented, not hidden): constant `cell_size*8/slot` bandwidth floor **and** ceiling each way (default 1440 B / 5 ms ≈ 2.3 Mbit/s). Every wrapping layer cuts MTU — default `cell_size` (1440) fits a full stealth-MTU (~1380) datagram and lands in the top obfs bucket (1472). *Not yet wired:* a config/control-plane transport selector to turn DAITA on per-deployment (that's Wave 2A, alongside mimic/quic).
- **2026-07-21 — Active-probe-resistant QUIC mimicry (authenticated Initial).** The QUIC Initial is now **authenticated**: its token carries `timestamp(8) ‖ nonce(16) ‖ keyed-BLAKE2-MAC(16)`, the MAC binding QUIC version + both connection IDs + timestamp + nonce under the shared obfs key (`mimicry::quic::{initial_packet(payload,key,now), verify_initial(dg,key,now) -> (nonce,payload), parse_short}`; new `blake2` dep). The QuicMimic transport verifies every long-header datagram and **silently drops** any Initial that fails the MAC (a *forged* probe — the prober lacks the key), is outside `±120s` (a *stale* replay), or whose nonce is already in a pruned replay cache (a *replayed* probe) — so the port looks dead to an active prober, not just to a passive fingerprinter. Short-header packets are unauthenticated (their payload is still gated by the WireGuard AEAD after deobfuscation); first contact must be a valid Initial. Verified without root: 8 `quic` unit tests (forged/tampered/stale/replay-nonce) + 3 transport tests (authenticated-initial-then-short delivers; forged-key Initial ignored; replayed Initial dropped) + the existing QUIC-mimicry capstone tunnel still passes. 71 tests, clippy + fmt clean.
  - Threat model: defeats **active probing** (replay a captured first packet / send a crafted QUIC Initial and watch for a distinguishing response). The obfs layer already gave passive unlinkability but is an unauthenticated stream cipher, so a forged/replayed Initial could reach the WG layer; the keyed Initial gates it at the transport. Assumes client/server clocks within 120s (NTP). *Next tier:* decoy-forwarding (proxy unauthenticated first-contact traffic to a real backend, e.g. an actual TLS/QUIC server) so even the *absence* of a normal response can't be used as a signal.
  - Decision: **authenticate only the Initial, not every packet** — active probing is a first-contact attack; per-packet MACs would duplicate the WG AEAD and cost MTU. Timestamp+nonce+MAC in the token (a realistic ~40-byte QUIC address-validation token) keeps the packet indistinguishable from real QUIC.

- **2026-07-21 — Mesh hybrid ("Tailscale, but actually private").** A user's own devices form a private WireGuard P2P overlay coordinated by the control plane. New mesh DTOs (`common::api`), a `mesh_devices` table + `POST /v1/mesh/register` (idempotent by pubkey, allocates a stable `/32` in `100.64.0.0/16`, returns your mesh IP + the account's peer list) and `GET /v1/mesh` (control-plane). `control-client` gained `mesh_register`/`mesh_list`; `client-core::resolve_mesh` builds a `Resolved` where the interface holds the mesh `/16` and each other device is a peer pinned to its mesh `/32` (no default route → normal egress untouched); `oxide-client mesh --endpoint host:port`. Verified without root: `mesh_flow` capstone — two devices register, discover each other, and a packet flows directly between them over the mesh (no server, no exit). 60 tests, clippy + fmt clean.
  - Decision: **the mesh peer list is account-scoped** (devices only mesh with same-account devices), but mesh IPs are globally unique within `100.64.0.0/16`. This is the "actually private" property — no shared coordination server sees cross-account topology.
  - Decision: **v1 uses reported endpoints (direct reachability)** — the joining device tells the control plane where it's reachable (`--endpoint`). NAT traversal (STUN-style hole punching / a DERP-style relay fallback) is deferred; today it assumes devices can reach each other's endpoints (public IP + forwarded UDP port, or same LAN).
  - Hybrid: run `mesh` for private device-to-device traffic **and** `connect` for an anonymous exit — the mesh carries only its `/16`, so it composes with a full-tunnel exit.

- **2026-07-21 — QUIC mimicry (stealth tier 2, UDP-native).** New `mimicry::quic` module + `Transport::QuicMimic` — the flow looks like an HTTP/3 (QUIC) session: a long-header Initial (QUIC v1, connection IDs, embedded ClientHello w/ SNI, padded to 1200) then short-header 1-RTT packets. UDP-native, so it avoids the TCP-over-TCP penalty of the TLS mimicry and blends into HTTP/3 traffic. Reuses the datagram transport model (first packet per peer = Initial, rest short). Verified: codec tests (looks like QUIC, round-trips, rejects garbage) + a capstone real WireGuard tunnel over QUIC mimicry on loopback. 59 tests, clippy + fmt clean.
  - Decision: **QUIC mimicry is the preferred stealth transport** (UDP-native like WireGuard, no TCP penalty, huge cover traffic); TLS-over-TCP is the fallback for UDP-hostile networks.

- **2026-07-21 — Stealth tier 2: TLS protocol mimicry.** New `mimicry` crate (TLS 1.3 handshake + record framing) and `wg-core::MimicTransport` (`Transport::Mimic`) that carries the tunnel over TCP inside a flow that looks like HTTPS — a censor sees a ClientHello (SNI = `www.cloudflare.com`), a ServerHello, and application_data records. Composes over the ChaCha obfs so record contents are high-entropy like real TLS. Verified without root: codec tests, a "client's first bytes are a TLS ClientHello" transport test, and a capstone running a real WireGuard tunnel over TLS-mimicry on loopback. 54 tests, clippy + fmt clean.
  - Decision: **mimicry, not real TLS** — endpoints don't validate the handshake; it only has to fool a *passive* censor. Security is the WG AEAD (+ obfs) inside. Rides TCP (real TLS is TCP) — the accepted TCP-over-TCP trade-off for a censorship fallback. Enabled `tokio` `io-util`.
  - Follow-up: wire a config/control-plane transport selector (`udp`/`mimic`) so daemons pick it (like the obfs key); the capability + capstone are done.

- **2026-07-21 — Client UX: TUI + privileged agent.** New crates: `client-core` (shared tunnel logic extracted from the CLI, with a controllable `stop` + `on_ready` stats callback), `oxide-agentd` (root agent serving a Unix-socket control API), `oxide-tui` (unprivileged ratatui client). `EngineStats` gained tx/rx bytes; `common::agent` holds the UI⇄agent protocol. Verified without root: agent socket round-trips (Status→disconnected, bad input→error) and the TUI's app-state logic is unit-tested; rendering + the connect path need a TTY/root. 47 tests, clippy + fmt clean.
  - Decision: **unprivileged UI + privileged agent split** (the M5 plan) — the TUI is a pure client of the control plane + the agent socket, so it never needs root. The tunnel runs *embedded* in the agent (not a supervised child), so status shows real throughput.

- **2026-07-21 — Observability: `/metrics`.** The control plane exposes a Prometheus text endpoint (`GET /metrics`): `oxide_accounts_total`, `oxide_servers_total`, `oxide_devices_total`, and per-server `oxide_server_active_peers{server=…}` / `oxide_server_capacity{…}`. Rendering is a testable function (`render_metrics`). Unauthenticated aggregate counts (no secrets) — firewall/scrape internally in production. 42 tests, clippy + fmt clean.

- **2026-07-21 — Post-quantum handshake (signature feature, core).** New `pq` crate wrapping ML-KEM-768. The KEM shared secret becomes the WireGuard PSK, so the tunnel is protected by x25519 **and** ML-KEM — quantum-resistant, and strictly additive (hybrid). Verified: KEM round-trip tests + a capstone running a real WireGuard tunnel keyed by the PQ-derived PSK. 40 tests, clippy + fmt clean.
  - Decision: **hybrid via the PSK slot**, not replacing x25519 — matches Mullvad's approach and means the not-yet-audited ML-KEM impl can only add security, never subtract. ML-KEM-768 = NIST category 3. Private key stored/transported as its 64-byte seed.
  - **Control-plane distribution (done, single-hop + multihop):** `oxide-serverd pq-genkey` prints a seed + public key; put the seed in the server config (`pq_private_seed`) and register the public key (`add-server --pq-public-key`). The client fetches the server's (or exit's) PQ key, encapsulates, sends the ciphertext at registration; the server/exit decapsulates from its peer list — both derive the same PSK. For multihop it keys to the **exit** and the entry relays the (already-obfuscated-and/or-PQ) bytes untouched. Capstones: `pq_flow` (CP-negotiated single-hop) and `multihop_flow` (multihop + relay + PQ together).

- **2026-07-21 — Hardening: SIGTERM + MSS clamping.** Daemons now shut down cleanly on `SIGTERM` (systemd/docker), not just Ctrl-C (`net-linux::shutdown_signal`). The NAT forward chain MSS-clamps forwarded TCP to the route MTU (`tcp flags syn ... maxseg size set rt mtu`), fixing the PMTU black-hole "ping works, curl hangs" bug — and it adapts to the lower MTU used under stealth. 36 tests, clippy + fmt clean.

- **2026-07-21 — Stealth mode (signature feature).** New `obfs` crate (ChaCha20 obfuscation codec) + `wg-core` `Transport` enum (`Plain` | `Obfuscated`). The engine now sends/receives over `Transport` instead of a raw `UdpSocket`; with an `obfuscation_key` set (config field, shared client/server), every datagram is wrapped so there's no WireGuard fingerprint on the wire and undecodable probes are silently dropped. `Engine::build`/`build_server` take a `Transport`. Verified without root: codec tests + a capstone that runs a real WireGuard tunnel entirely over the obfuscated transport. 34 tests, clippy + fmt clean.
  - Decision: **obfs4/Shadowsocks-style keystream obfuscation** (defeats fingerprint-based blocking, which is how WG gets blocked), not protocol mimicry yet. Full TLS/QUIC mimicry is the next stealth tier. It's obfuscation, not AEAD — the real security is the WireGuard layer underneath.
  - Decision: `Transport` as an enum (not a generic) to avoid threading another type parameter through the engine.
  - Note: obfuscation adds ~14+padding bytes/packet — lower the tunnel MTU (~1380) when using it. Obfs key is a 32-byte base64 (generate with `oxide-serverd genkey`).
  - **Control-plane distribution (done):** `oxide-control-plane add-server --obfuscation-key <b64>` stores it; registration returns it; `oxide-client connect` uses it automatically. Multihop works transparently — the key is the *exit's* (tunnel terminates there), and the entry relays the obfuscated bytes untouched, so no relay change was needed.

- **2026-07-21 — Hardening pass 2: netlink + IPv6.** `net-linux` link/addr/route now go through **rtnetlink** (real netlink) instead of `ip` shell-outs — a `Netlink` handle (async, cloneable). `bring_up_interface` is async, returns the link index, and assigns both `address` and the new `address6`. **IPv6 inside the tunnel** works: dual-stack tunnel addresses and `::/0` full-tunnel routing (`::/1` + `8000::/1`). The netns test now pings both IPv4 and IPv6 across the tunnel (CI verifies). 29 tests, clippy + fmt clean.
  - Decision: migrate **mutations** to netlink; keep the single default-route *read* as `ip route show` (reconstructing it from raw netlink dumps is disproportionately fiddly for a low-risk read). nftables (NAT/kill switch) still shells out to `nft` — nft-via-netlink is a separate follow-up.
  - Scope: WG **transport** is still IPv4 (WG-over-IPv6 + control-plane v6 IP allocation are follow-ups); IPv6 *inside* the tunnel is done.

- **2026-07-21 — Hardening pass 1.** (1) **DoS defense:** a shared boringtun `RateLimiter` is now fed to every server peer's `Tunn` (via `Engine::build_server`, 100 handshakes/s) with a 1s `reset_count` tick — cookie challenges engage under handshake floods. (2) **Auth rate-limiting:** per-IP fixed-window limiter (60 req/60s) as axum middleware, plus a 64 KB body cap and 15s request timeout; served with connect-info so the limiter sees client IPs. Protects the account-number bearer auth from brute force. (3) **CI:** `.github/workflows/ci.yml` runs fmt + clippy `-D warnings` + test, `cargo-deny` (advisories/bans/sources), and a **privileged `netns` job** that runs `scripts/netns-test.sh` as root — this is how the live-kernel TUN/nft path gets verified without local sudo (runs on push). `deny.toml` added. 28 tests, clippy clean, fmt clean.
  - Next hardening chunk: migrate `net-linux` off `ip`/`nft` shell-outs to real netlink/nftables; IPv6; MSS clamping.

- **2026-07-21 — Multihop built.** New `relay` crate (tokio UDP proxy). The client runs a single WireGuard session keyed to the **exit** but sends ciphertext to the **entry**, which relays it — so no single server sees both ends, and the client engine/`wg-core` needed **zero changes**. Control plane: `relays` table + per-entry port allocation, `POST /v1/devices/multihop` (registers device on exit, ensures relay route, returns exit-key + entry-relay-endpoint), `GET /v1/internal/servers/:id/relays`. `oxide-serverd` runs a relay per route (fetched on poll); `oxide-client connect --exit/--entry`. Verified without root: relay forwarding + flow isolation, control-plane multihop registration, and a capstone `multihop_flow` test driving a real tunnel client→relay→exit. 27 tests, clippy clean.
  - Decision: **WireGuard-native multihop (entry relays ciphertext), not onion encryption.** Matches Mullvad; keeps the data plane single-encryption and untouched. The entry is a dumb UDP forwarder that can't read the traffic (it's encrypted to the exit).
  - Decision: entry auto-picks the least-loaded server ≠ exit when `--entry` is omitted.

- **2026-07-21 — M4 privacy (leak prevention) built.** Kill switch + DNS leak protection in `net-linux` (pure ruleset/resolv.conf builders, unit-tested; applied via nft/resolv.conf, needs root). Control plane hands out a per-server DNS in device registration. `oxide-client` gains `--kill-switch` and applies/tears down DNS + kill switch around the tunnel in the right order. No-logs posture audited (data plane keeps nothing on disk, no info-level PII). 24 tests, clippy clean. Multihop deferred to its own milestone (nested tunnels + entry/exit pairing is substantial).
  - Decision: kill switch is **opt-in** (`--kill-switch`) in v0 to avoid surprising lockouts from a CLI; a real client would default it on with a toggle.
  - Decision: manage `/etc/resolv.conf` directly for v0 (simple, auditable); resolved/NetworkManager backends later.

- **2026-07-21 — M3 multi-server selection built.** Servers carry location (country/city) and a soft `capacity`; they heartbeat live load (`active_peers`, derived from boringtun handshake recency via `EngineHandle::stats`) to the control plane. New `GET /v1/servers/best?country=&city=` picks the least-loaded healthy server (load factor = active/capacity); `oxide-client connect` auto-selects when `--server` is omitted (with `--country`/`--city` filters). `oxide-serverd`'s poll loop now also heartbeats. Verified without root (selection test + HTTP smoke test showing the pick flip as load shifts). 20 tests, clippy clean.
  - Decision: **load = live heartbeated active-peer count**, not registered device count — registrations aren't connections. A server that has never heartbeated is treated as healthy (may not run the loop); staleness (>90s) applies once it starts.
  - Note: `wg-core` stayed API-additive (just `stats()`), as the roadmap intended.

- **2026-07-21 — M2 control plane v0 built.** New crates `control-plane` (axum + sqlx/SQLite) and `control-client`. Anonymous account numbers (Mullvad-style, no PII), device registration with per-server tunnel-IP allocation, and a token-authed peer-list endpoint. `wg-core` peer table is now runtime-mutable (`EngineHandle::reconcile`); `oxide-serverd` polls the control plane and reconciles live; `oxide-client connect` registers a device and connects. Verified end-to-end without root: control-plane API test, runtime-reconcile test, and a capstone `control_plane_flow` test that drives a real tunnel from a control-plane-provisioned device. 19 tests, clippy clean.
  - Decision: **anonymous account numbers** — the number is the whole credential; no email/PII, minimal stored identity (on-brand for the Mullvad privacy goal).
  - Decision: **SQLite for v0, Postgres for a fleet.** SQLite is single-writer/single-node; fine and zero-infra now, but a multi-node API tier (M6) needs a networked DB. All access is via sqlx runtime queries so the swap is mechanical.
  - Decision: **server polls the control plane** (rather than CP pushing) — keeps the data plane a pull-based, stateless-on-disk consumer that holds peers in RAM only.
  - Decision: **`control-plane` never depends on `wg-core`** (enforced by layering); shared wire types live in `common::api`.

- **2026-07-21 — M1 built.** Ripped out the old TCP-echo `src/` and rebuilt as a WireGuard-based workspace (boringtun 0.7). New crates: common, wg-core, net-linux, oxide-serverd, oxide-client. Real handshake, TUN devices, allowed-IPs routing, full-tunnel NAT. Key derivation verified byte-identical to `wg pubkey`.
  - Decision: **build on boringtun, not hand-rolled crypto** — matches what NordLynx/Mullvad actually ship; our value is the control/privacy/ops layer.
  - Decision: **shell out to `ip`/`nft` for M1** (like wg-quick); migrate to netlink/nftables libraries in M5. Only the TUN `ioctl` is in-process.
  - Decision: `common` owns the `TunQueue` trait so `wg-core` stays OS-agnostic and unit-testable without root.
