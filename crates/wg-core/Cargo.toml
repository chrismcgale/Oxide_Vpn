[package]
name = "oxide-wg-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "oxide_wg_core"
path = "src/lib.rs"

[features]
# Exposes in-memory TUN test helpers for this crate's and downstream integration tests.
test-util = []

[dev-dependencies]
oxide-pq = { path = "../pq" }

[dependencies]
oxide-common = { path = "../common" }
oxide-obfs = { path = "../obfs" }
oxide-mimicry = { path = "../mimicry" }
boringtun = { workspace = true }
x25519-dalek = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
thiserror = { workspace = true }
ipnet = { workspace = true }
